module.exports = {
	PORT: 3000
	, LOG_NAME: 'debug.log'
	, UNPROCESSABLE_ENTITY_ERROR_MESSAGE: 'Could not process what you posted - probably bad headers (not application/json) or a bad schema (posted data needs to have a field type, which contains a string).'
}